name: Build and Publish Docker Images Stage

on:
    push:
        branches: stage

jobs:
    workspace-api:
        runs-on: ubuntu-22.04
        steps:
            - name: Checkout
              uses: actions/checkout@v3

            - name: Free disk space
              run: |
                df -h /
                sudo swapoff -a
                sudo apt clean
                docker image ls -aq
                docker rmi $(docker image ls -aq)
                find ~/work/_temp -name "cache.tgz" -exec rm -f {} \;
                sudo rm -rf "/usr/local/share/boost"
                sudo rm -rf "$AGENT_TOOLSDIRECTORY"
                df -h /

            - name: Set up QEMU
              uses: docker/setup-qemu-action@v2

            - name: Set up Docker Buildx
              uses: docker/setup-buildx-action@v2

            - name: Login to DockerHub
              uses: docker/login-action@v2
              with:
                  username: ${{ secrets.DOCKERHUB_USERNAME }}
                  password: ${{ secrets.DOCKERHUB_TOKEN }}

            - name: Login to GitHub Container Registry
              uses: docker/login-action@v2
              with:
                  registry: ghcr.io
                  username: ${{ github.repository_owner }}
                  password: ${{ secrets.GH_TOKEN }}



            - name: Build
              uses: docker/build-push-action@v3
              with:
                  context: .
                  file: ./.deploy/api/Dockerfile
                  load: true
                  platforms: linux/amd64
                  tags: |
                      ghcr.io/workspace-vn/workspace-api-stage:latest
                      workspacevn/workspace-api-stage:latest

                  cache-from: type=registry,ref=workspacevn/workspace-api-stage:latest
                  cache-to: type=inline

            - name: Docker images list
              run: |
                 sudo docker image list

            - name: Push to Docker Hub Registry
              run: |
                  docker push workspacevn/workspace-api-stage:latest



            - name: Push to Github Registry
              run: |
                  docker push ghcr.io/workspace-vn/workspace-api-stage:latest

    workspace-webapp:
        runs-on: ubuntu-22.04
        steps:
            - name: Checkout
              uses: actions/checkout@v3

            - name: Free disk space
              run: |
                df -h /
                sudo swapoff -a
                sudo apt clean
                docker image ls -aq
                docker rmi $(docker image ls -aq)
                find ~/work/_temp -name "cache.tgz" -exec rm -f {} \;
                sudo rm -rf "/usr/local/share/boost"
                sudo rm -rf "$AGENT_TOOLSDIRECTORY"
                df -h /

            - name: Set up QEMU
              uses: docker/setup-qemu-action@v2

            - name: Set up Docker Buildx
              uses: docker/setup-buildx-action@v2

            - name: Login to DockerHub
              uses: docker/login-action@v2
              with:
                  username: ${{ secrets.DOCKERHUB_USERNAME }}
                  password: ${{ secrets.DOCKERHUB_TOKEN }}

            - name: Login to GitHub Container Registry
              uses: docker/login-action@v2
              with:
                  registry: ghcr.io
                  username: ${{ github.repository_owner }}
                  password: ${{ secrets.GH_TOKEN }}



            - name: Build and push
              uses: docker/build-push-action@v3
              with:
                  context: .
                  file: ./.deploy/webapp/Dockerfile
                  load: true
                  tags: |
                      ghcr.io/workspace-vn/workspace-webapp-stage:latest
                      workspacevn/workspace-webapp-stage:latest

                  cache-from: type=registry,ref=workspacevn/workspace-webapp-stage:latest
                  cache-to: type=inline

            - name: Push to Docker Hub Registry
              run: |
                  docker push workspacevn/workspace-webapp-stage:latest



            - name: Push to Github Registry
              run: |
                  docker push ghcr.io/workspace-vn/workspace-webapp-stage:latest
